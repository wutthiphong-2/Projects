# 📖 คู่มือทดสอบ API แบบละเอียด

## 🎯 เป้าหมาย
ทดสอบว่า endpoint `/api/api-keys/activity-log` ทำงานได้ถูกต้องหรือไม่

---

## 📋 สิ่งที่ต้องเตรียม

### 1. ตรวจสอบว่า Backend ทำงานอยู่

**วิธีตรวจสอบ:**
1. เปิด Command Prompt หรือ PowerShell
2. พิมพ์คำสั่ง:
   ```bash
   curl http://127.0.0.1:8000/
   ```
   
   **หรือเปิด Browser ไปที่:**
   ```
   http://127.0.0.1:8000/
   ```

3. **ถ้าได้ response แบบนี้ แสดงว่า backend ทำงาน:**
   ```json
   {
     "message": "AD Management API",
     "version": "1.0.0",
     "status": "running"
   }
   ```

4. **ถ้าไม่ได้ response หรือ connection refused:**
   - ต้อง start backend ก่อน
   - เปิด Command Prompt/PowerShell
   - พิมพ์:
     ```bash
     cd backend
     python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
     ```
   - รอจนเห็นข้อความ: `INFO:     Uvicorn running on http://127.0.0.1:8000`

---

## 🚀 วิธีที่ 1: ใช้ FastAPI Swagger UI (แนะนำ - ง่ายที่สุด)

### ขั้นตอนที่ 1: เปิด Swagger UI

1. **เปิด Browser** (Chrome, Edge, Firefox ฯลฯ)
2. **พิมพ์ URL นี้ใน address bar:**
   ```
   http://127.0.0.1:8000/docs
   ```
   หรือ
   ```
   http://localhost:8000/docs
   ```

3. **คุณจะเห็นหน้า Swagger UI** ที่มีรายการ API endpoints ทั้งหมด

---

### ขั้นตอนที่ 2: Login เพื่อรับ JWT Token

**ทำไมต้อง Login?**
- เพราะ endpoint `/api/api-keys/activity-log` ต้องการ authentication
- ต้องมี JWT token เพื่อยืนยันตัวตน

**วิธีทำ:**

1. **หาหมวด "auth"** (มักอยู่ด้านบน)
2. **คลิกที่ `POST /api/auth/login`**
   - จะเห็นกล่องสีเทาแสดงรายละเอียด
3. **คลิกปุ่ม "Try it out"** (สีน้ำเงิน อยู่ด้านขวาบน)
   - กล่องจะเปลี่ยนเป็นสีขาว พร้อม form ให้กรอก
4. **กรอกข้อมูล:**
   ```json
   {
     "username": "admin",
     "password": "password123"
   }
   ```
   - แก้ไข username และ password ให้ตรงกับระบบของคุณ
5. **คลิกปุ่ม "Execute"** (สีน้ำเงิน อยู่ด้านล่าง)
6. **ดูผลลัพธ์:**
   - Scroll ลงไปดูส่วน "Responses"
   - **ถ้าสำเร็จ (200):** จะเห็น response แบบนี้:
     ```json
     {
       "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
       "token_type": "bearer",
       "user": {
         "username": "admin",
         "dn": "CN=admin,CN=Users,DC=example,DC=com"
       }
     }
     ```
   - **คัดลอกค่า `access_token`** (ตัวอักษรยาวๆ เริ่มด้วย `eyJ...`)
   - **เก็บไว้ใช้ในขั้นตอนถัดไป**

---

### ขั้นตอนที่ 3: Authorize (ใส่ Token)

**ทำไมต้อง Authorize?**
- เพื่อให้ Swagger UI ส่ง token ไปกับทุก request อัตโนมัติ
- ไม่ต้องใส่ token ทุกครั้งที่ทดสอบ

**วิธีทำ:**

1. **หาปุ่ม "Authorize" 🔒** (อยู่ด้านบนขวาของหน้า)
2. **คลิกปุ่ม "Authorize"**
   - จะมี popup ขึ้นมา
3. **ในช่อง "Value":**
   - พิมพ์: `Bearer ` (มีช่องว่างหลัง Bearer)
   - ตามด้วย token ที่คัดลอกมา
   - **ตัวอย่าง:**
     ```
     Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJhZG1pbiIsImV4cCI6MTcwNDE2ODAwMH0.xxxxx
     ```
4. **คลิก "Authorize"**
5. **คลิก "Close"**
   - ตอนนี้ทุก request จะส่ง token ไปอัตโนมัติแล้ว

---

### ขั้นตอนที่ 4: ทดสอบ Activity Log Endpoint

1. **หาหมวด "api-keys"** (scroll ลงไป)
2. **ห endpoint `GET /api/api-keys/activity-log`**
3. **คลิกที่ endpoint นี้**
   - จะเห็นรายละเอียด
4. **คลิก "Try it out"**
5. **กรอก Parameters (optional):**
   - `limit`: `100` (จำนวนรายการสูงสุด)
   - `offset`: `0` (เริ่มจากรายการที่เท่าไหร่)
   - `action_type`: (เว้นว่าง = ทุกประเภท)
   - `user`: (เว้นว่าง = ทุก user)
   - `api_key_id`: (เว้นว่าง = ทุก API key)
6. **คลิก "Execute"**
7. **ดูผลลัพธ์:**

   **ถ้าสำเร็จ (200):**
   ```json
   {
     "activities": [
       {
         "id": 1,
         "timestamp": "2024-01-01T10:00:00+07:00",
         "action_type": "CREATE_KEY",
         "api_key_id": "abc123",
         "api_key_name": "My API Key",
         "user": "admin",
         "details": "Created API key: My API Key",
         "ip_address": "127.0.0.1"
       }
     ],
     "total": 1
   }
   ```

   **ถ้าได้ 404:**
   - ตรวจสอบว่า backend restart แล้วหรือยัง
   - ตรวจสอบ route order ใน `backend/app/routers/api_keys.py`

   **ถ้าได้ 401:**
   - Token หมดอายุ หรือไม่ถูกต้อง
   - Login ใหม่และใส่ token ใหม่

---

## 🐍 วิธีที่ 2: ใช้ Python Script

### ขั้นตอนที่ 1: ตรวจสอบว่า Python ติดตั้งแล้ว

1. **เปิด Command Prompt หรือ PowerShell**
2. **พิมพ์:**
   ```bash
   python --version
   ```
   - ควรเห็น: `Python 3.x.x`
3. **ถ้าไม่มี Python:**
   - ติดตั้ง Python จาก https://www.python.org/downloads/

---

### ขั้นตอนที่ 2: ตรวจสอบว่า requests library ติดตั้งแล้ว

1. **พิมพ์:**
   ```bash
   python -c "import requests; print('OK')"
   ```
2. **ถ้าได้ error:**
   ```bash
   pip install requests
   ```

---

### ขั้นตอนที่ 3: รัน Script

1. **เปิด Command Prompt หรือ PowerShell**
2. **ไปที่โฟลเดอร์โปรเจค:**
   ```bash
   cd D:\Projects
   ```
3. **รัน script:**
   ```bash
   python test_api_activity_log.py
   ```
4. **Script จะถาม username และ password:**
   ```
   Username: admin
   Password: password123
   ```
5. **รอผลลัพธ์:**
   - Script จะ login อัตโนมัติ
   - เรียก API activity log
   - แสดงผลลัพธ์

**หรือใส่ username/password ใน command line:**
```bash
python test_api_activity_log.py admin password123
```

---

## 💻 วิธีที่ 3: ใช้ cURL (Command Line)

### สำหรับ Windows PowerShell:

**Step 1: Login**
```powershell
$loginBody = @{
    username = "admin"
    password = "password123"
} | ConvertTo-Json

$loginResponse = Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/auth/login" `
    -Method Post `
    -Body $loginBody `
    -ContentType "application/json"

$token = $loginResponse.access_token
Write-Host "Token: $token"
```

**Step 2: เรียก Activity Log API**
```powershell
$headers = @{
    Authorization = "Bearer $token"
}

$response = Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/api-keys/activity-log?limit=100&offset=0" `
    -Method Get `
    -Headers $headers

$response | ConvertTo-Json -Depth 10
```

---

### สำหรับ Linux/Mac หรือ Git Bash:

**Step 1: Login**
```bash
TOKEN=$(curl -X POST "http://127.0.0.1:8000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"password123"}' \
  | jq -r '.access_token')

echo "Token: $TOKEN"
```

**Step 2: เรียก Activity Log API**
```bash
curl -X GET "http://127.0.0.1:8000/api/api-keys/activity-log?limit=100&offset=0" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" | jq .
```

---

## 🌐 วิธีที่ 4: ใช้ Browser DevTools (สำหรับ Frontend)

### ขั้นตอนที่ 1: เปิด Frontend

1. **เปิด Browser**
2. **ไปที่หน้า API Management:**
   ```
   http://localhost:3000/api-management
   ```
   (หรือ port ที่ frontend รันอยู่)

---

### ขั้นตอนที่ 2: เปิด DevTools

1. **กด `F12`** หรือ **คลิกขวา > Inspect**
2. **ไปที่ tab "Network"** (เครือข่าย)

---

### ขั้นตอนที่ 3: ดู API Calls

1. **Refresh หน้า** หรือ **ทำ action ที่เรียก API**
2. **ดูใน Network tab:**
   - จะเห็น request ต่างๆ
   - **หาที่ชื่อ `/api/api-keys/activity-log`**
3. **คลิกที่ request นั้น**
4. **ดูรายละเอียด:**
   - **Headers:** ดู Authorization header
   - **Response:** ดู JSON response
   - **Status:** ควรเป็น 200 (สำเร็จ)

---

## ❌ แก้ไขปัญหาที่พบบ่อย

### ปัญหา 1: Connection Refused

**อาการ:**
```
Failed to connect to 127.0.0.1:8000
Connection refused
```

**วิธีแก้:**
1. ตรวจสอบว่า backend ทำงานอยู่:
   ```bash
   netstat -ano | findstr :8000
   ```
2. ถ้าไม่มี process:
   ```bash
   cd backend
   python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
   ```

---

### ปัญหา 2: 404 Not Found

**อาการ:**
```
404 Not Found
GET /api/api-keys/activity-log
```

**วิธีแก้:**
1. **ตรวจสอบ route order:**
   - เปิด `backend/app/routers/api_keys.py`
   - ตรวจสอบว่า `@router.get("/activity-log")` อยู่ก่อน `@router.get("/{key_id}")`
2. **Restart backend:**
   - หยุด backend (Ctrl+C)
   - Start ใหม่

---

### ปัญหา 3: 401 Unauthorized

**อาการ:**
```
401 Unauthorized
Could not validate credentials
```

**วิธีแก้:**
1. **Token หมดอายุ:**
   - Login ใหม่
   - เอา token ใหม่
2. **Token ไม่ถูกต้อง:**
   - ตรวจสอบว่าใส่ `Bearer ` (มีช่องว่าง) ก่อน token
   - ตรวจสอบว่า token ไม่มีช่องว่างหรือตัวอักษรแปลกๆ

---

### ปัญหา 4: 500 Internal Server Error

**อาการ:**
```
500 Internal Server Error
Failed to get activity log
```

**วิธีแก้:**
1. **ดู backend logs:**
   - ดู terminal ที่รัน backend
   - หา error message
2. **ตรวจสอบ database:**
   - ตรวจสอบว่า `api_activity_log.db` ถูกสร้างแล้ว
   - ตรวจสอบ permissions

---

## ✅ Checklist ก่อนทดสอบ

- [ ] Backend server ทำงานอยู่ (ตรวจสอบที่ `http://127.0.0.1:8000/`)
- [ ] มี username และ password สำหรับ login
- [ ] Route `/activity-log` อยู่ก่อน `/{key_id}` ใน `api_keys.py`
- [ ] Backend restart แล้วหลังแก้ไข route

---

## 📝 สรุป

**วิธีที่แนะนำ:**
1. **Swagger UI** - ง่ายที่สุด ไม่ต้องเขียน code
2. **Python Script** - สะดวกสำหรับทดสอบบ่อยๆ
3. **cURL** - ใช้ใน automation หรือ CI/CD
4. **Browser DevTools** - ดีสำหรับ debug frontend

**เริ่มต้นด้วย Swagger UI ก่อน** เพราะใช้งานง่ายและเห็นผลลัพธ์ทันที!

