# 🌐 คู่มือเปิดให้คนอื่นเข้าถึงได้ (Network Access)

## 📋 สรุปการเปลี่ยนแปลง

✅ **Backend**: แก้ไขให้ listen ที่ `0.0.0.0` แล้ว (สามารถเข้าถึงได้จากเครื่องอื่น)
✅ **CORS**: ตั้งค่าให้รองรับทุก origin แล้ว
✅ **Frontend**: สามารถแก้ไข API URL ได้ง่าย

---

## 🚀 วิธีตั้งค่า

### ขั้นตอนที่ 1: หา IP Address ของเครื่องที่รัน Backend

#### Windows:
```powershell
# วิธีที่ 1: ใช้ ipconfig
ipconfig

# หา IPv4 Address (เช่น 192.168.1.100)
# หรือ IPv6 Address (เช่น fe80::xxxx)
```

#### Linux/Mac:
```bash
ifconfig
# หรือ
ip addr
```

**ตัวอย่าง IP ที่พบ:**
- `192.168.1.100` (IPv4 - แนะนำ)
- `10.0.0.50` (IPv4)
- `172.16.0.10` (IPv4)

---

### ขั้นตอนที่ 2: ตั้งค่า Frontend

มี 2 วิธี:

#### วิธีที่ 1: แก้ไขไฟล์ config.js (แนะนำ - ง่ายที่สุด)

1. เปิดไฟล์ `frontend/src/config.js`
2. หาบรรทัดที่มี `const SERVER_IP = 'localhost';`
3. แก้ไขเป็น IP address ของเครื่อง server:

```javascript
// ก่อนแก้ไข:
const SERVER_IP = 'localhost';

// หลังแก้ไข (ตัวอย่าง):
const SERVER_IP = '192.168.1.100';
```

**ตัวอย่างอื่น:**
```javascript
const SERVER_IP = '10.0.0.50';      // IPv4
const SERVER_IP = '172.16.0.10';    // IPv4
```

4. **สำหรับ Development:**
   - Restart frontend (กด Ctrl+C แล้วรัน `npm start` อีกครั้ง)

5. **สำหรับ Production:**
   - Build frontend ใหม่:
   ```bash
   cd frontend
   npm run build
   ```

#### วิธีที่ 2: ใช้ Environment Variable (สำหรับ Production)

1. สร้างไฟล์ `.env` ในโฟลเดอร์ `frontend/`:
```env
REACT_APP_API_URL=http://192.168.1.100:8000
```

2. Build:
```bash
cd frontend
npm run build
```

---

### ขั้นตอนที่ 3: ตรวจสอบ Firewall

⚠️ **สำคัญ!** ต้องเปิด Firewall ให้ port 8000 (backend) และ 3000 (frontend)

#### Windows Firewall:

1. เปิด **Windows Defender Firewall**
2. กด **Advanced Settings**
3. กด **Inbound Rules** → **New Rule**
4. เลือก **Port** → **Next**
5. เลือก **TCP** → ระบุ port `8000` → **Next**
6. เลือก **Allow the connection** → **Next**
7. เลือก Profile ทั้งหมด → **Next**
8. ตั้งชื่อ "AD Management Backend" → **Finish**

ทำซ้ำสำหรับ port `3000` (frontend)

#### หรือใช้ PowerShell (รันเป็น Administrator):
```powershell
# เปิด port 8000 (Backend)
New-NetFirewallRule -DisplayName "AD Management Backend" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow

# เปิด port 3000 (Frontend)
New-NetFirewallRule -DisplayName "AD Management Frontend" -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

---

## 🎯 วิธีใช้งาน

### สถานการณ์ที่ 1: ทุกคนเข้าผ่าน Frontend (Development)

1. **เครื่อง Server** (เครื่องที่รัน backend):
   ```bash
   # Terminal 1: รัน Backend
   cd backend
   python run_uvicorn_local.py
   
   # Terminal 2: รัน Frontend
   cd frontend
   npm start
   ```

2. **เครื่อง Client** (เครื่องอื่น):
   - เปิด browser ไปที่: `http://SERVER_IP:3000`
   - ตัวอย่าง: `http://192.168.1.100:3000`

### สถานการณ์ที่ 2: ทุกคนเข้าผ่าน Frontend (Production Build)

1. **เครื่อง Server**:
   ```bash
   # รัน Backend
   cd backend
   python run_uvicorn_local.py
   
   # Build และ Serve Frontend
   cd frontend
   npm run build
   # ใช้ nginx, serve package, หรือ static server
   ```

2. **เครื่อง Client**:
   - เปิด browser ไปที่: `http://SERVER_IP:PORT`
   - (ขึ้นอยู่กับ web server ที่ใช้ serve build)

---

## 🔒 Security Considerations

⚠️ **สำหรับ Production:**

1. **อย่าใช้ CORS `allow_origins=["*"]`** 
   - แก้ไขใน `backend/app/main.py`:
   ```python
   app.add_middleware(
       CORSMiddleware,
       allow_origins=["http://YOUR_FRONTEND_URL"],  # แทนที่ *
       allow_credentials=True,
       allow_methods=["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
       allow_headers=["*"],
   )
   ```

2. **ใช้ HTTPS** ใน production
3. **ตั้งค่า Authentication** ให้เข้มงวด
4. **ใช้ Environment Variables** สำหรับ sensitive data

---

## ✅ ทดสอบการเชื่อมต่อ

### ทดสอบจากเครื่องอื่น:

```bash
# ทดสอบ Backend
curl http://SERVER_IP:8000/api/health

# ควรได้ response:
# {"status":"healthy","timestamp":"...","version":"1.0.0"}
```

### ทดสอบ Frontend:
- เปิด browser ไปที่ `http://SERVER_IP:3000`
- ควรเห็นหน้า Login หรือ Dashboard

---

## 🐛 แก้ปัญหา

### ปัญหา: ไม่สามารถเชื่อมต่อได้

1. **ตรวจสอบ Backend รันอยู่:**
   ```bash
   # ที่เครื่อง Server
   netstat -an | findstr :8000
   # ควรเห็น 0.0.0.0:8000 หรือ [::]:8000
   ```

2. **ตรวจสอบ Firewall:**
   - ตรวจสอบว่าเปิด port แล้ว
   - ลองปิด Firewall ชั่วคราวเพื่อทดสอบ

3. **ตรวจสอบ IP Address:**
   - ใช้ `ping SERVER_IP` จากเครื่อง Client
   - ตรวจสอบว่าเครื่องอยู่ใน network เดียวกัน

4. **ตรวจสอบ Frontend Config:**
   - เปิด Developer Tools (F12)
   - ดู Network tab ว่า API calls ไปที่ IP ที่ถูกต้อง

### ปัญหา: CORS Error

- Backend ตั้งค่า CORS แล้ว (`allow_origins=["*"]`)
- ถ้ายังมีปัญหา ให้ตรวจสอบว่า backend รันที่ `0.0.0.0` (ไม่ใช่ `127.0.0.1`)

---

## 📝 สรุป

✅ **Backend**: รันที่ `0.0.0.0:8000` → พร้อมรับ connection จาก network
✅ **Frontend**: แก้ไข `config.js` ให้ชี้ไปที่ IP ของ backend
✅ **Firewall**: เปิด port 8000 และ 3000
✅ **CORS**: ตั้งค่าแล้ว

**พร้อมใช้งานแล้ว!** 🎉

