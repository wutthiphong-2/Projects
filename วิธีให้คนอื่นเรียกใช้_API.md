# 🔑 วิธีให้คนอื่นเรียกใช้ API ของคุณ

## ❓ คำถาม: "คนอื่นจะเอาส่วนไหนถึงจะสามารถเรียกข้อมูลเราได้?"

**คำตอบสั้นๆ:** 
- **ต้องมี API Key** (สร้างจากหน้า API Management)
- **ส่ง API Key ใน Header `X-API-Key`**
- **รู้ URL ของ API server**
- **รู้ endpoint ที่ต้องการเรียก**

---

## 📋 สิ่งที่คนอื่นต้องมี (3 อย่างหลัก)

### 1️⃣ **API Key** (สำคัญที่สุด!)

**คืออะไร?**
- ตัวเลข/ตัวอักษรยาวๆ สำหรับยืนยันตัวตน
- ตัวอย่าง: `ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
- **แสดงแค่ครั้งเดียว** ตอนสร้างหรือ regenerate

**ได้มาจากไหน?**
- **คุณต้องสร้างให้เขา** ผ่านหน้า API Management
- หรือให้เขา login เข้าระบบ แล้วสร้างเอง

**วิธีสร้าง API Key ให้คนอื่น:**
1. Login เข้าระบบ
2. ไปหน้า **API Management**
3. คลิก **"สร้าง API Key"**
4. กรอกข้อมูล:
   - **ชื่อ:** เช่น "API Key for John"
   - **Permissions:** เลือกสิทธิ์ที่ต้องการให้
   - **Rate Limit:** ตั้งค่าการใช้งาน
5. คลิก **"สร้าง"**
6. **คัดลอก API Key** (จะแสดงแค่ครั้งเดียว!)
7. **ส่ง API Key ให้เขา** (ทาง Email, Chat, etc.)
   - ⚠️ **ระวัง:** ใช้ช่องทางที่ปลอดภัย!

---

### 2️⃣ **URL ของ API Server**

**คืออะไร?**
- ที่อยู่ของ API server
- ตัวอย่าง: `http://127.0.0.1:8000` หรือ `https://api.yourcompany.com`

**บอกเขายังไง?**
- แจ้ง URL ที่ backend รันอยู่
- ถ้า production อาจเป็น domain เช่น `https://api.example.com`

---

### 3️⃣ **Endpoint ที่ต้องการเรียก**

**คืออะไร?**
- Path ของ API endpoint
- ตัวอย่าง: `/api/users/`, `/api/groups/`, `/api/ous/`

**บอกเขายังไง?**
- แจ้ง endpoint ที่ต้องการให้เรียก
- หรือให้เขาไปดูที่ Tab "API Endpoints" ในหน้า API Management

---

## 🔐 วิธีใช้งาน (สำหรับคนอื่น)

### **วิธีที่ 1: ใช้ cURL**

```bash
curl -X GET "http://127.0.0.1:8000/api/users/" \
  -H "X-API-Key: ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

**อธิบาย:**
- `-X GET` = HTTP Method (GET, POST, PUT, DELETE)
- `"http://127.0.0.1:8000/api/users/"` = URL + Endpoint
- `-H "X-API-Key: ..."` = **ส่ง API Key ใน Header**

---

### **วิธีที่ 2: ใช้ Python**

```python
import requests

# ตั้งค่า
API_URL = "http://127.0.0.1:8000"
API_KEY = "ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
ENDPOINT = "/api/users/"

# เรียก API
url = f"{API_URL}{ENDPOINT}"
headers = {
    "X-API-Key": API_KEY  # ← ส่ง API Key ใน Header
}

response = requests.get(url, headers=headers)
data = response.json()

print(data)
```

---

### **วิธีที่ 3: ใช้ JavaScript/Node.js**

```javascript
const API_URL = "http://127.0.0.1:8000";
const API_KEY = "ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx";

fetch(`${API_URL}/api/users/`, {
  method: 'GET',
  headers: {
    'X-API-Key': API_KEY  // ← ส่ง API Key ใน Header
  }
})
.then(res => res.json())
.then(data => console.log(data))
.catch(error => console.error('Error:', error));
```

---

### **วิธีที่ 4: ใช้ Postman**

1. เปิด Postman
2. สร้าง request ใหม่
3. Method: `GET`
4. URL: `http://127.0.0.1:8000/api/users/`
5. ไปที่ tab **"Headers"**
6. เพิ่ม:
   - **Key:** `X-API-Key`
   - **Value:** `ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
7. คลิก **"Send"**

---

## 📝 ตัวอย่างการใช้งานจริง

### **ตัวอย่าง 1: ดึงข้อมูลผู้ใช้ทั้งหมด**

**Request:**
```bash
curl -X GET "http://127.0.0.1:8000/api/users/" \
  -H "X-API-Key: ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

**Response:**
```json
[
  {
    "dn": "CN=John Doe,CN=Users,DC=example,DC=com",
    "cn": "John Doe",
    "sAMAccountName": "johndoe",
    "mail": "john.doe@example.com",
    ...
  }
]
```

---

### **ตัวอย่าง 2: ดึงข้อมูลกลุ่มทั้งหมด**

**Request:**
```bash
curl -X GET "http://127.0.0.1:8000/api/groups/" \
  -H "X-API-Key: ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
```

---

### **ตัวอย่าง 3: สร้างผู้ใช้ใหม่ (ต้องมี permission `users:write`)**

**Request:**
```bash
curl -X POST "http://127.0.0.1:8000/api/users/" \
  -H "X-API-Key: ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "cn": "Jane Doe",
    "sAMAccountName": "janedoe",
    "mail": "jane.doe@example.com",
    "department": "IT",
    "password": "SecurePassword123!"
  }'
```

---

## ⚠️ สิ่งที่ต้องระวัง

### 1. **API Key Security:**
- ❌ **อย่าแชร์ API Key** ในที่สาธารณะ
- ❌ **อย่า commit API Key** ลง Git
- ✅ **เก็บ API Key** ใน Environment Variables
- ✅ **ใช้ HTTPS** ใน production

### 2. **Permissions:**
- API Key มี permissions (สิทธิ์)
- ตรวจสอบว่า API Key มี permission สำหรับ endpoint นั้นหรือไม่
- ถ้าไม่มี → ได้ error **403 Forbidden**

### 3. **Rate Limiting:**
- API Key มี rate limit (ต่อนาที/ต่อชั่วโมง)
- ถ้าเกิน limit → ได้ error **429 Too Many Requests**
- ตรวจสอบ rate limit ในหน้า API Management

### 4. **API Key Expiry:**
- API Key อาจหมดอายุ
- ถ้าหมดอายุ → ได้ error **401 Unauthorized**
- ตรวจสอบวันหมดอายุในหน้า API Management

---

## ✅ Checklist สำหรับคุณ (ผู้ให้ API)

เมื่อจะให้คนอื่นใช้ API:

- [ ] สร้าง API Key ใหม่ให้เขา
- [ ] ตั้ง Permissions ตามที่ต้องการ
- [ ] ตั้ง Rate Limit ตามที่ต้องการ
- [ ] ตั้งวันหมดอายุ (optional)
- [ ] คัดลอก API Key และส่งให้เขา
- [ ] แจ้ง URL ของ API server
- [ ] แจ้ง endpoints ที่ต้องการให้เรียก
- [ ] แจ้งวิธีการใช้งาน (ตัวอย่าง code)

---

## ✅ Checklist สำหรับคนอื่น (ผู้ใช้ API)

เมื่อได้รับ API Key:

- [ ] เก็บ API Key ในที่ปลอดภัย
- [ ] เก็บใน Environment Variables (ไม่ hardcode)
- [ ] ทดสอบ API Key ว่าทำงานหรือไม่
- [ ] ตรวจสอบ Permissions ว่ามีสิทธิ์เข้าถึง endpoint ที่ต้องการหรือไม่
- [ ] ตรวจสอบ Rate Limit
- [ ] ตรวจสอบวันหมดอายุ

---

## 📊 สรุป

### **คนอื่นต้องการ:**
1. ✅ **API Key** (คุณสร้างให้)
2. ✅ **URL ของ API** (คุณแจ้งให้)
3. ✅ **Endpoint ที่ต้องการเรียก** (คุณแจ้งให้)
4. ✅ **ตัวอย่างการใช้งาน** (คุณแจ้งให้)

### **วิธีส่ง API Key:**
1. ส่งผ่าน **Email** (ถ้ามี email integration)
2. ส่งผ่าน **Chat/Slack** (ช่องทางที่ปลอดภัย)
3. ส่งผ่าน **Password Manager** (ถ้าใช้)
4. ⚠️ **อย่าส่งผ่าน public channels!**

### **ตัวอย่าง Email ที่ส่งให้:**

```
Subject: API Key สำหรับเข้าถึง AD Management API

สวัสดี,

นี่คือ API Key สำหรับเข้าถึง AD Management API:

API Key: ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
API URL: http://127.0.0.1:8000
Permissions: users:read, groups:read
Rate Limit: 100 requests/minute, 1000 requests/hour
Expires: ไม่หมดอายุ

ตัวอย่างการใช้งาน (cURL):
curl -X GET "http://127.0.0.1:8000/api/users/" \
  -H "X-API-Key: ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

ตัวอย่างการใช้งาน (Python):
import requests
url = "http://127.0.0.1:8000/api/users/"
headers = {"X-API-Key": "ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"}
response = requests.get(url, headers=headers)
print(response.json())

⚠️ ข้อควรระวัง:
- เก็บ API Key ในที่ปลอดภัย
- อย่าแชร์กับคนอื่น
- อย่า commit ลง Git

หากมีปัญหา ติดต่อ: your-email@example.com
```

---

## 🎯 สรุปสั้นๆ

**ให้คนอื่นเรียกใช้ API ได้ต้อง:**

1. **คุณต้อง:**
   - สร้าง API Key ให้เขา
   - แจ้ง URL ของ API
   - แจ้ง endpoints ที่ต้องการ
   - ส่ง API Key ไปให้เขา

2. **เขาต้อง:**
   - รับ API Key จากคุณ
   - ส่ง API Key ใน Header `X-API-Key`
   - เรียก API ด้วย URL + Endpoint
   - เก็บ API Key ในที่ปลอดภัย

---

## 💡 ตัวอย่างการใช้งานจริง

### **สถานการณ์:** คุณต้องการให้เพื่อนใช้งาน API เพื่อดึงข้อมูลผู้ใช้

**ขั้นตอน:**

1. **คุณทำ:**
   - Login เข้าระบบ
   - ไปหน้า API Management
   - สร้าง API Key ชื่อ "API Key for Friend"
   - เลือก Permission: `users:read`
   - คัดลอก API Key: `ak_abc123...`
   - ส่งให้เพื่อนทาง Email

2. **เพื่อนทำ:**
   - รับ API Key จาก Email
   - ใช้ Python script:
   ```python
   import requests
   api_key = "ak_abc123..."  # จาก Email
   url = "http://127.0.0.1:8000/api/users/"
   headers = {"X-API-Key": api_key}
   response = requests.get(url, headers=headers)
   print(response.json())
   ```
   - ✅ ได้ข้อมูลผู้ใช้แล้ว!

---

**สรุป:** คนอื่นต้องมี **API Key** เท่านั้น และส่งใน Header `X-API-Key` เมื่อเรียก API!

