# 📖 คู่มือการใช้งานหน้า API Management

## 🎯 ภาพรวม

หน้า API Management เป็นเครื่องมือสำหรับจัดการ API Keys, ดูสถิติการใช้งาน, ดูรายการ Endpoints, จัดการ JWT Tokens, และติดตาม Activity Log

---

## 🚀 วิธีเข้าถึง

### 1. Login เข้าระบบ
1. เปิด Browser ไปที่: `http://localhost:3000` (หรือ URL ที่ frontend รันอยู่)
2. กรอก **Username** และ **Password**
3. คลิก **"Login"**

### 2. เข้าหน้า API Management
**วิธีที่ 1: ผ่าน Sidebar Menu**
- ดูที่ **Sidebar ด้านซ้าย**
- หาข้อความ **"API Management"** (มี icon 🔑)
- **คลิก** ที่ "API Management"

**วิธีที่ 2: ผ่าน URL**
- พิมพ์ URL ใน address bar:
  ```
  http://localhost:3000/api-management
  ```
- กด Enter

---

## 📋 Tabs ต่างๆ ในหน้า API Management

### 1️⃣ Tab: **API Keys** 🔑

**ฟังก์ชัน:**
- สร้าง API Key ใหม่
- แก้ไข API Key
- ลบ API Key
- เปิด/ปิดใช้งาน API Key
- Regenerate API Key (สร้างใหม่)
- ส่ง API Key ทาง Email

**วิธีใช้งาน:**

#### สร้าง API Key ใหม่:
1. คลิกปุ่ม **"สร้าง API Key"** (มุมขวาบน)
2. กรอกข้อมูล:
   - **ชื่อ:** เช่น "Production API Key", "Development Key"
   - **คำอธิบาย:** อธิบายการใช้งาน (optional)
   - **Rate Limit (ต่อนาที):** จำนวน requests สูงสุดต่อนาที (default: 60)
   - **Rate Limit (ต่อชั่วโมง):** จำนวน requests สูงสุดต่อชั่วโมง (default: 1000)
   - **วันหมดอายุ:** เลือกวันหมดอายุ (optional - ถ้าไม่เลือก = ไม่หมดอายุ)
   - **Permissions:** เลือกสิทธิ์ที่ต้องการ (ถ้าไม่เลือก = read-only)
   - **ส่งอีเมลแจ้งเตือน:** ติ๊กถ้าต้องการส่ง email
   - **อีเมล:** กรอก email ที่ต้องการส่ง (ถ้าติ๊กส่งอีเมล)
3. คลิก **"สร้าง"**
4. **สำคัญ:** คัดลอก API Key ที่แสดง (จะแสดงแค่ครั้งเดียว!)
5. เก็บ API Key ไว้ในที่ปลอดภัย

#### แก้ไข API Key:
1. หา API Key ที่ต้องการแก้ไขในตาราง
2. คลิกปุ่ม **"แก้ไข"** (icon ✏️)
3. แก้ไขข้อมูลที่ต้องการ
4. คลิก **"บันทึก"**

#### ลบ API Key:
1. หา API Key ที่ต้องการลบ
2. คลิกปุ่ม **"ลบ"** (icon 🗑️)
3. ยืนยันการลบ

#### เปิด/ปิดใช้งาน:
1. หา API Key ที่ต้องการ
2. ใช้ **Switch** ในคอลัมน์ "สถานะ"
3. คลิกเพื่อเปิด/ปิด

#### Regenerate API Key:
1. หา API Key ที่ต้องการ
2. คลิกปุ่ม **"Regenerate"** (icon 🔄)
3. **สำคัญ:** คัดลอก API Key ใหม่ (API Key เก่าจะใช้ไม่ได้แล้ว!)

#### ส่ง API Key ทาง Email:
1. หา API Key ที่ต้องการ
2. คลิกปุ่ม **"Send Email"** (icon ✉️)
3. กรอก:
   - **อีเมลผู้รับ:** email ที่ต้องการส่ง
   - **ข้อความ:** ข้อความเพิ่มเติม (optional)
4. คลิก **"ส่ง"**

---

### 2️⃣ Tab: **Statistics** 📊

**ฟังก์ชัน:**
- ดูสถิติการใช้งาน API
- ดูจำนวน requests
- ดูกราฟการใช้งาน

**วิธีใช้งาน:**
1. คลิก Tab **"Statistics"**
2. ดูข้อมูล:
   - **Total Requests:** จำนวน requests ทั้งหมด
   - **Active Keys:** จำนวน API Keys ที่ใช้งานอยู่
   - **Daily Usage:** ตารางแสดงการใช้งานรายวัน

---

### 3️⃣ Tab: **API Endpoints** 🌐

**ฟังก์ชัน:**
- ดูรายการ API Endpoints ทั้งหมด
- ค้นหา endpoint
- Filter ตาม Method หรือ Tag
- ดูรายละเอียด endpoint
- คัดลอก Path

**วิธีใช้งาน:**

#### ดูรายการ Endpoints:
1. คลิก Tab **"API Endpoints"**
2. จะเห็นตารางแสดง endpoints ทั้งหมด

#### ค้นหา Endpoint:
1. ใช้ช่อง **"ค้นหา endpoint..."** ด้านบน
2. พิมพ์ชื่อ path หรือ description
3. กด Enter หรือรอ auto-search

#### Filter:
1. ใช้ **Dropdown "Filter by Method":**
   - เลือก GET, POST, PUT, DELETE, PATCH
2. ใช้ **Dropdown "Filter by Tag":**
   - เลือก tag เช่น auth, users, api-keys

#### ดูรายละเอียด:
1. คลิกปุ่ม **"ดูรายละเอียด"** (icon ℹ️)
2. จะเห็น Modal แสดง:
   - Method, Path, Description
   - Parameters (ถ้ามี)
   - ตัวอย่างการใช้งาน (cURL, Python, JavaScript)

#### คัดลอก Path:
1. คลิกปุ่ม **"คัดลอก Path"** (icon 📋)
2. Path จะถูกคัดลอกไปยัง clipboard

---

### 4️⃣ Tab: **JWT Tokens** 🔐

**ฟังก์ชัน:**
- ดู Active JWT Tokens
- ดูสถานะ Token (Active/Inactive)
- ดูวันหมดอายุ
- Revoke Token (ยกเลิกการใช้งาน)

**วิธีใช้งาน:**

#### ดู Active Tokens:
1. คลิก Tab **"JWT Tokens"**
2. จะเห็นตารางแสดง tokens ทั้งหมด

#### ดูรายละเอียด Token:
- **Token Hash:** hash ของ token
- **สร้างเมื่อ:** วันเวลาที่สร้าง
- **หมดอายุเมื่อ:** วันเวลาที่หมดอายุ
- **ใช้งานล่าสุด:** วันเวลาที่ใช้งานล่าสุด
- **IP Address:** IP ที่ใช้ token
- **สถานะ:** Active หรือ Inactive

#### Revoke Token:
1. หา Token ที่ต้องการ revoke
2. คลิกปุ่ม **"Revoke"** (icon 🗑️)
3. ยืนยันการ revoke
4. Token จะไม่สามารถใช้งานได้อีก

#### Revoke All Tokens:
1. คลิกปุ่ม **"Revoke All My Tokens"** (มุมขวาบน)
2. ยืนยันการ revoke
3. **คำเตือน:** คุณจะต้อง login ใหม่หลังจาก revoke!

---

### 5️⃣ Tab: **Activity Log** 📝

**ฟังก์ชัน:**
- ดูประวัติการใช้งาน API Keys
- ดูกิจกรรมต่างๆ เช่น CREATE_KEY, SEND_EMAIL, etc.
- ดู IP Address และ User ที่ทำกิจกรรม

**วิธีใช้งาน:**
1. คลิก Tab **"Activity Log"**
2. จะเห็นตารางแสดง activities ทั้งหมด
3. ข้อมูลที่แสดง:
   - **Timestamp:** วันเวลาที่เกิดกิจกรรม
   - **Action Type:** ประเภทกิจกรรม
   - **API Key Name:** ชื่อ API Key (ถ้ามี)
   - **User:** ผู้ใช้ที่ทำกิจกรรม
   - **Details:** รายละเอียด
   - **IP Address:** IP ที่ทำกิจกรรม

---

## 🔑 วิธีใช้ API Key

### 1. ใช้กับ cURL:
```bash
curl -X GET "http://127.0.0.1:8000/api/users/" \
  -H "X-API-Key: YOUR_API_KEY_HERE"
```

### 2. ใช้กับ Python:
```python
import requests

url = "http://127.0.0.1:8000/api/users/"
headers = {
    "X-API-Key": "YOUR_API_KEY_HERE"
}

response = requests.get(url, headers=headers)
print(response.json())
```

### 3. ใช้กับ JavaScript:
```javascript
fetch('http://127.0.0.1:8000/api/users/', {
  method: 'GET',
  headers: {
    'X-API-Key': 'YOUR_API_KEY_HERE'
  }
})
.then(res => res.json())
.then(data => console.log(data));
```

---

## ⚠️ ข้อควรระวัง

### 1. **API Key Security:**
- ❌ **อย่าแชร์ API Key** กับคนอื่น
- ❌ **อย่า commit API Key** ลง Git
- ✅ **เก็บ API Key** ในที่ปลอดภัย
- ✅ **ใช้ Environment Variables** สำหรับเก็บ API Key

### 2. **Rate Limiting:**
- API Key มี rate limit (ต่อนาที/ต่อชั่วโมง)
- ถ้าเกิน limit จะได้ error 429 (Too Many Requests)
- ตรวจสอบ rate limit ใน Tab "API Keys"

### 3. **Token Expiry:**
- JWT Tokens มีวันหมดอายุ
- ตรวจสอบวันหมดอายุใน Tab "JWT Tokens"
- Login ใหม่เพื่อรับ token ใหม่

### 4. **Permissions:**
- API Key มี permissions (สิทธิ์)
- ตรวจสอบ permissions ก่อนใช้งาน
- ถ้าไม่มี permission จะได้ error 403 (Forbidden)

---

## ❓ คำถามที่พบบ่อย (FAQ)

### Q: API Key หายไป ต้องทำยังไง?
**A:** API Key จะแสดงแค่ครั้งเดียวตอนสร้างหรือ regenerate เท่านั้น ถ้าหายไป:
1. ไปที่ Tab "API Keys"
2. คลิก "Regenerate" ที่ API Key นั้น
3. คัดลอก API Key ใหม่
4. **เก็บไว้ในที่ปลอดภัย!**

### Q: API Key หมดอายุแล้ว ต้องทำยังไง?
**A:** 
1. ไปที่ Tab "API Keys"
2. สร้าง API Key ใหม่ หรือ
3. Regenerate API Key เก่า (ถ้ายังใช้งานได้)

### Q: ใช้ API Key แล้วได้ error 401 (Unauthorized)?
**A:** ตรวจสอบ:
1. API Key ถูกต้องหรือไม่
2. API Key ยัง Active อยู่หรือไม่
3. API Key ยังไม่หมดอายุ
4. Header ชื่อ "X-API-Key" ถูกต้อง

### Q: ใช้ API Key แล้วได้ error 429 (Too Many Requests)?
**A:** 
- คุณเกิน rate limit แล้ว
- รอสักครู่แล้วลองใหม่
- หรือตรวจสอบ rate limit ใน Tab "API Keys"

### Q: ใช้ API Key แล้วได้ error 403 (Forbidden)?
**A:**
- API Key ไม่มี permission สำหรับ endpoint นั้น
- ตรวจสอบ permissions ใน Tab "API Keys"
- แก้ไข permissions หรือสร้าง API Key ใหม่

### Q: ต้องการส่ง API Key ให้คนอื่น?
**A:**
1. ไปที่ Tab "API Keys"
2. คลิก "Send Email" ที่ API Key นั้น
3. กรอก email ผู้รับ
4. คลิก "ส่ง"

---

## 📞 วิธีขอความช่วยเหลือ

### ถ้ามีปัญหา:
1. ตรวจสอบ **Activity Log** ดูว่ามี error อะไร
2. ตรวจสอบ **Console** (กด F12) ดู error messages
3. ตรวจสอบว่า **Backend ทำงานอยู่** (`http://127.0.0.1:8000/`)
4. ตรวจสอบว่า **Frontend ทำงานอยู่** (`http://localhost:3000`)

---

## ✅ Checklist สำหรับผู้ใช้ใหม่

- [ ] Login เข้าระบบได้แล้ว
- [ ] เข้าหน้า API Management ได้
- [ ] สร้าง API Key ใหม่
- [ ] คัดลอก API Key และเก็บไว้ในที่ปลอดภัย
- [ ] ทดสอบใช้ API Key กับ API endpoint
- [ ] ดู Statistics
- [ ] ดู Activity Log
- [ ] เข้าใจวิธีการใช้งานแต่ละ Tab

---

## 🎯 สรุป

หน้า API Management มี 5 Tabs หลัก:
1. **API Keys** - จัดการ API Keys
2. **Statistics** - ดูสถิติการใช้งาน
3. **API Endpoints** - ดูรายการ endpoints
4. **JWT Tokens** - จัดการ tokens
5. **Activity Log** - ดูประวัติการใช้งาน

**เริ่มต้นใช้งาน:**
1. Login เข้าระบบ
2. ไปหน้า API Management
3. สร้าง API Key ใหม่
4. ใช้ API Key กับ API endpoints

**สำคัญ:** เก็บ API Key ไว้ในที่ปลอดภัยและอย่าแชร์กับคนอื่น!

