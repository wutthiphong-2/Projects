# 📋 วิธีทดสอบหน้า API Management

## 🎯 วัตถุประสงค์
ทดสอบว่า API Management page ทำงานได้ครบถ้วนและถูกต้อง

---

## 📝 **วิธีที่ 1: ทดสอบด้วย Python Script (แนะนำ)**

### ขั้นตอน:

1. **เปิดไฟล์ `test_api_management.py`**

2. **แก้ไขตั้งค่า:**
   ```python
   API_URL = "http://127.0.0.1:8000"
   USERNAME = "your_username"  # ← ใส่ username
   PASSWORD = "your_password"  # ← ใส่ password
   ```

3. **รัน script:**
   ```bash
   python test_api_management.py
   ```

4. **ดูผลลัพธ์:**
   - Script จะทดสอบ API endpoints ทั้งหมดที่หน้า API Management ใช้
   - แสดงผลลัพธ์ทีละส่วน
   - ตรวจสอบว่ามี Error หรือไม่

### สิ่งที่ทดสอบ:
- ✅ API Keys endpoints (List, Create, Get, Scopes)
- ✅ API Usage endpoints (Stats, By Key)
- ✅ API Endpoints discovery
- ✅ Activity Log endpoints
- ✅ JWT Tokens endpoints

---

## 📋 **วิธีที่ 2: ทดสอบด้วย Checklist (Manual Testing)**

### ขั้นตอน:

1. **เปิดไฟล์ `test_api_management_page.md`**

2. **ทดสอบทีละ Tab ตาม checklist:**

   #### **Tab 1: API Keys**
   - สร้าง API Key ใหม่
   - แก้ไข API Key
   - Regenerate API Key
   - Toggle Active/Inactive
   - ลบ API Key
   - ส่งอีเมล
   - Copy API Key

   #### **Tab 2: Statistics**
   - ดูสถิติการใช้งาน
   - ดูสถิติแต่ละ API Key

   #### **Tab 3: Endpoints**
   - ดูรายการ Endpoints
   - ดูรายละเอียด Endpoint
   - Copy Path

   #### **Tab 4: JWT Tokens**
   - ดูรายการ Active Tokens
   - Revoke Token
   - Revoke All Tokens

   #### **Tab 5: Activity Log**
   - ดู Activity Log
   - ใช้ Filter และ Pagination

3. **เช็ค ✅ ใน checklist** เมื่อทดสอบผ่าน

---

## 🔍 **วิธีที่ 3: ทดสอบการทำงานจริง (Integration Test)**

### ขั้นตอน:

1. **เปิดหน้า API Management:**
   ```
   http://localhost:3000/api-management
   ```

2. **Login เข้าระบบ**

3. **ทดสอบสร้าง API Key:**
   - ไปที่ Tab "API Keys"
   - คลิก "สร้าง API Key"
   - กรอกข้อมูล:
     - ชื่อ: "Test API Key"
     - Permissions: เลือก `users:read`, `groups:read`
   - คลิก "สร้าง"
   - คัดลอก API Key

4. **ทดสอบใช้ API Key:**
   ```bash
   # เปิดไฟล์ test_api_example.py
   # แก้ไข API_KEY = "your_api_key_here"
   python test_api_example.py
   ```

5. **ตรวจสอบว่า API ทำงานได้:**
   - ✅ ดึงข้อมูลผู้ใช้ได้
   - ✅ ดึงข้อมูลกลุ่มได้ (ถ้ามี permission)
   - ❌ ไม่สามารถสร้างผู้ใช้ได้ (ถ้าไม่มี permission)

---

## 🧪 **ทดสอบ Edge Cases**

### 1. **ทดสอบ Permissions:**
- สร้าง API Key โดยเลือก `users:read` เท่านั้น
- ทดสอบเรียก API:
  ```bash
  # ✅ ควรสำเร็จ
  GET /api/users/ → 200 OK
  
  # ❌ ควรได้ Error
  POST /api/users/ → 403 Forbidden
  GET /api/groups/ → 403 Forbidden
  ```

### 2. **ทดสอบ Rate Limit:**
- สร้าง API Key โดยตั้ง Rate Limit: 10 req/min
- เรียก API 15 ครั้งติดต่อกัน:
  ```bash
  # ครั้งที่ 1-10: ✅ 200 OK
  # ครั้งที่ 11-15: ❌ 429 Too Many Requests
  ```

### 3. **ทดสอบ Expired API Key:**
- สร้าง API Key โดยตั้งวันหมดอายุเป็นวันนี้
- ทดสอบใช้ API Key → ควรได้ Error 401/403

### 4. **ทดสอบ Inactive API Key:**
- สร้าง API Key
- Toggle เป็น Inactive
- ทดสอบใช้ API Key → ควรได้ Error 401/403

---

## 📱 **ทดสอบ UI/UX**

### 1. **ทดสอบ Responsive Design:**
- เปิดหน้า API Management บนมือถือ → ตรวจสอบว่า Layout ไม่แตก
- เปิดบนหน้าจอใหญ่ → ตรวจสอบว่าใช้งานสะดวก

### 2. **ทดสอบ User Experience:**
- ตรวจสอบว่า Error Messages ชัดเจน
- ตรวจสอบว่า Loading States แสดงถูกต้อง
- ตรวจสอบว่า Confirmations ทำงานถูกต้อง

### 3. **ทดสอบ Browser Compatibility:**
- Chrome/Edge
- Firefox
- Safari (ถ้ามี)

---

## 🐛 **ทดสอบ Error Handling**

### 1. **เมื่อ Backend ไม่ทำงาน:**
- หยุด Backend Server
- เปิดหน้า API Management
- ตรวจสอบว่ามี Error Message แสดง

### 2. **เมื่อ Network Error:**
- Disconnect Internet
- ทดสอบใช้งานหน้า API Management
- ตรวจสอบว่ามี Error Message แสดง

### 3. **เมื่อได้รับ Error จาก API:**
- ทดสอบใช้ API Key ที่ไม่มี permission
- ตรวจสอบว่ามี Error Message แสดงถูกต้อง

---

## ✅ **Checklist สรุป**

### Backend API:
- [ ] API Keys endpoints ทำงาน
- [ ] API Usage endpoints ทำงาน
- [ ] API Endpoints discovery ทำงาน
- [ ] Activity Log endpoints ทำงาน
- [ ] JWT Tokens endpoints ทำงาน

### Frontend UI:
- [ ] ทุก Tab แสดงข้อมูลได้
- [ ] CRUD operations ทำงาน
- [ ] Modal และ Forms ทำงาน
- [ ] Error handling ทำงาน
- [ ] Loading states ทำงาน

### Integration:
- [ ] API Key ที่สร้างใช้งานได้จริง
- [ ] Permissions ทำงานถูกต้อง
- [ ] Rate Limit ทำงาน
- [ ] Activity Log บันทึกได้

---

## 📞 **รายงานปัญหา**

ถ้าพบปัญหา ให้บันทึก:
1. **รายละเอียดปัญหา:** อธิบายว่าพบปัญหาอะไร
2. **ขั้นตอนการเกิดปัญหา:** ทำอะไรแล้วเกิดปัญหา
3. **Error Message:** Copy Error Message หรือ Screenshot
4. **Browser และ OS:** Chrome/Windows, Safari/Mac, etc.

---

## 🎉 **เมื่อทดสอบผ่าน**

เมื่อทุกอย่างทำงานได้:
- ✅ หน้า API Management พร้อมใช้งาน
- ✅ API endpoints ทำงานถูกต้อง
- ✅ Permissions และ Rate Limit ทำงาน
- ✅ UI/UX ใช้งานสะดวก

**พร้อมให้ผู้ใช้ใช้งาน!** 🚀

